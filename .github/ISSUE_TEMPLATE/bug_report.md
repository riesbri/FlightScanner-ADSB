---
name: Bug report
about: Something isn't working as expected
labels: bug
---

**Describe the bug**
A clear description of what went wrong.

**To reproduce**
Steps to reproduce the behaviour.

**Expected behaviour**
What you expected to happen.

**Logs**
Paste relevant log output (run `journalctl --user -u flightscanner -n 100` or add `-Dlogging.level.com.flightscanner=DEBUG` to the JVM args).

**Environment**
- OS / platform:
- Java version (`java -version`):
- dump1090-fa version:
- `airport.code` / `airport.radius.nm` in use:
