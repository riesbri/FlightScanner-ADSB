## Flight Scraper Java Framework

Flight Scraper currently using **Java 21** // **Playwright** // **SQLite**

brew services start mysql

mysql -u root
brew services start mysql
brew services stop mysql

CREATE USER 'webadmin'@'localhost' IDENTIFIED BY 'whatPassword!';
GRANT ALL PRIVILEGES ON flight_tracker.* TO 'webadmin'@'localhost';
instead of root for webadmin!
brew services restart httpd

richi: passwordXDrichi!
webadmin: whatPassword123
